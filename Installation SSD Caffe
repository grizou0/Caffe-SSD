# Creation d'un répertoire dans /opt
Exemple :  
cd opt/intel
# installation du package caffe
git clone https://github.com/weiliu89/caffe.git
cd caffe
git checkout ssd
# Creation du fichier de compilation Makefile.config
cp Makefile.config.example Makefile.config
et on modifie les options dans ce fichier Makefile.config
(voir référence dans installation CPU ou GPU)
USE_CUDNN := 1                    #pour activer le mode GPU
CUDA_DIR := /usr/local/cuda-10.2  #chemin cuda
Suivant la version cuda, 
CUDA_ARCH := -gencode arch=compute_30,code=sm_30 \
             -gencode arch=compute_35,code=sm_35 \
             -gencode arch=compute_50,code=sm_50 \
             -gencode arch=compute_52,code=sm_52 \
             -gencode arch=compute_61,code=sm_61 \
# Uncomment to support layers written in Python (will link against Python libs)
WITH_PYTHON_LAYER := 1
# Whatever else you find you need goes here.
INCLUDE_DIRS := $(PYTHON_INCLUDE) /usr/local/include /usr/include/hdf5/serial/
LIBRARY_DIRS := $(PYTHON_LIB) /usr/local/lib /usr/lib /usr/lib/x86_64-linux-gnu$
USE_PKG_CONFIG := 1 #liaison OpenCV si l'installation opencv est avec openvino



Modification du chemin cuda par cuda-10.2 (suivant la version cuda).
https://github.com/grizou0/Caffe-SSD/blob/master/Installation%20GPU

# Compilation
# -----------
make clean
make all
make py
make test
optionnel:
make runtest
Si tout est correcte, on passe à la creation de la base de donnée.
